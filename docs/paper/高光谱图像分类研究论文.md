# 面向可复现高光谱图像分类的 HybridSN 基线与多路线比较框架

> 论文草稿状态：阶段 3.2 首个正式 HybridSN baseline 版本（2026-08-17）
>
> 作者信息：待填写
>
> 说明：统一 seed=1442 的 `paper30` HybridSN 正式 baseline 已完成；多随机种子、空间块划分、改进与消融结果仍为待实验项。本文不采用参考论文数字或第三方预训练权重代填结果。

## 摘要

高光谱图像同时包含丰富的光谱信息与空间信息，但其波段维数高、类别分布不均衡，并且数据划分与统计变换方式容易引入乐观偏差。为建立可复现且可公平比较的课程实验基线，本文以 Pavia University 数据集为首个研究对象，设计配置驱动的数据处理与 HybridSN 实验框架。该框架固定用于论文兼容复现的 `paper30` 和用于模型选择的 `fair24_6_70` 两种分层随机划分，仅利用训练中心像元拟合逐波段标准化与 PCA，并仅利用训练光谱和训练标签拟合 LDA，最后通过统一接口输出中心像元特征或 25×25 光谱—空间邻域块。本文按原论文实现三层 3D 卷积、3D→2D 特征重排、一层 2D 卷积和全连接分类头，Pavia 输入输出为 `N×1×15×25×25 → N×9`，共有 4,844,793 个参数。统一 seed=1442 的 `paper30` 正式实验固定训练 100 epochs，不使用测试集选模，得到 OA 99.9532%、AA 99.8790% 和 Kappa 0.999380；训练耗时 494.36 s，测试端到端推理吞吐为 12,692 samples/s。空间审计进一步发现，100% 测试 patch 含训练中心像元且 100% 含同类训练中心像元，说明该高精度只适合作为随机像元协议 baseline，不能代表严格跨区域泛化。后续将在 `fair24_6_70` 上开展多随机种子、空间块划分、模型改进与公平比较。

**关键词：** 高光谱图像分类；HybridSN；主成分分析；光谱—空间特征；数据泄漏；可复现实验

## 1 引言

高光谱遥感影像在连续窄波段上记录地物反射响应，使单个像元可由一条高维光谱向量描述。相比普通 RGB 图像，高光谱数据能够区分颜色相近但材料组成不同的地物，已广泛应用于农业监测、城市地物识别和环境分析。然而，高维光谱也带来波段冗余、样本不均衡、空间上下文利用不足和计算开销较大等问题。

传统高光谱分类通常将降维、人工空间特征和分类器分开设计。例如，PCA 或 LDA 用于降低光谱维数，LBP 或 Gabor 特征用于描述局部纹理，SVM 或梯度提升树用于完成分类。深度学习方法则通过卷积网络直接学习特征。Roy 等提出的 HybridSN 使用连续 3D 卷积联合提取光谱—空间特征，再以 2D 卷积学习更高层空间表示，在控制三维卷积复杂度的同时保留联合建模能力 [1]。

教师课程任务依次要求完成 PyTorch 环境与高光谱数据处理、HybridSN 分类复现、HybridSN 改进以及其他模型的多数据集比较。若不同路线分别编写数据代码，容易出现划分不一致、标签错位或测试集参与拟合等问题。因此，本文首先建立统一的数据身份和预处理接口，使不同光谱变换、空间表示及分类器共享同一坐标、标签和测试集；随后以独立模型模块复现 HybridSN 原论文结构，并通过可恢复 CLI 完成首个正式训练—推理—评价闭环。本文当前工作的主要贡献如下：

1. 固定并验证 Pavia University 的论文兼容划分与公平比较划分，保存逐样本二维坐标、原始标签和完整元数据；
2. 实现只在训练中心像元上拟合的标准化与 PCA，以及只使用训练光谱和训练标签拟合的 LDA，阻断验证集和测试集信息泄漏；
3. 设计 `none/PCA/LDA × pixel/patch` 的可配置数据接口，使 PCA15 与 LDA8 可通过 YAML 的 `reducer` 字段一行切换；
4. 为传统分类器和 PyTorch 深度模型提供共享 sample ID、坐标和标签契约，为后续多路线、多随机种子比较奠定基础；
5. 按 Pavia University 的 15 波段、25×25 输入复现 HybridSN 3D–2D 特征层级，并通过逐层 shape、参数量、logits 和交叉熵反传测试固定模型契约。
6. 完成统一 seed=1442 的 100 epochs 正式 baseline，输出精度、性能和可视化，并用空间重叠审计约束随机像元高分的解释边界。

## 2 相关方法

### 2.1 光谱降维与特征选择

PCA 是一种无监督线性降维方法，通过寻找最大化样本方差的正交方向，将原始波段映射为较少的主成分 [2]。它不直接利用类别标签，适合降低光谱冗余，但最大方差方向未必等同于最具类别判别力的方向。LDA 则利用类别标签，使类间散度相对类内散度尽可能大 [3]；对于含 (C) 个类别的数据，其输出维数不超过 (C-1)，因此 Pavia University 的 LDA 输出至多为 8 维。波段选择不构造线性组合，而是保留部分真实波段，通常具有更强的物理可解释性。

### 2.2 空间特征表示

仅使用中心像元光谱向量会忽略邻域纹理和形状。LBP 通过比较邻域灰度关系编码局部纹理 [4]；Gabor 滤波器通过不同尺度与方向的带通响应描述空间频率特征 [5]。另一类方法直接提取固定尺寸邻域块，并由卷积神经网络学习空间结构。本文首个深度基线采用 25×25 邻域，该尺寸与 HybridSN 在 Pavia University 上使用的输入空间范围一致 [1]。

### 2.3 传统分类器与深度模型

SVM 通过最大间隔决策边界处理高维小样本分类，已广泛用于高光谱遥感 [6]。XGBoost 通过加法树模型与正则化目标函数实现可扩展梯度提升 [7]。HybridSN 则将 3D CNN 与 2D CNN 串联：前者联合建模光谱与空间信息，后者进一步抽象空间特征，最后由全连接分类头输出类别 logits [1]。对于当前单标签九分类任务，训练采用交叉熵损失；模型内部不必显式添加 Softmax，推理阶段再将 logits 转换为概率。

## 3 数据与方法

### 3.1 数据集与标签

Pavia University 数据立方体尺寸为 610×340×103，分别表示空间高度、空间宽度和光谱波段数。标签图尺寸为 610×340，其中 0 为背景或未标注像元，1–9 分别对应 Asphalt、Meadows、Gravel、Trees、Painted metal sheets、Bare Soil、Bitumen、Self-Blocking Bricks 和 Shadows。共有 42,776 个有标签像元参与训练、验证和测试。

### 3.2 固定数据划分

本文统一使用 `seed=1442` 按类别标签分层随机划分。首先将全部有标签像元划分为 30% 候选训练池和 70% 公共测试集，然后构造两套协议：

- `paper30`：12,832 个训练样本、无验证样本、29,944 个测试样本，用于尽量对齐原论文的 30%/70% 训练测试设置 [1]；
- `fair24_6_70`：将候选训练池再次按类别分层为 10,265 个训练样本和 2,567 个验证样本，同时保持相同的 29,944 个测试样本，用于调参与公平比较。

两套协议共享完全相同的测试集，且 `fair24_6_70` 的训练集与验证集合并后严格等于 `paper30` 的训练集。每个样本保存零基二维坐标 $(r,c)$、原始标签 $y\in\{1,\ldots,9\}$ 和 split 索引。测试集仅用于最终评价，不参与超参数或最佳轮次选择。

### 3.3 训练集专用标准化

设训练中心像元的光谱矩阵为 $X_{tr}\in\mathbb{R}^{N_{tr}\times D}$，其中 $D=103$。对第 $d$ 个波段，仅使用训练样本计算均值与总体标准差：

\[
\mu_d=\frac{1}{N_{tr}}\sum_{i=1}^{N_{tr}}x_{id},\qquad
\sigma_d=\sqrt{\frac{1}{N_{tr}}\sum_{i=1}^{N_{tr}}(x_{id}-\mu_d)^2}.
\]

任意样本按

\[
\tilde{x}_{id}=\frac{x_{id}-\mu_d}{\sigma_d}
\]

进行变换。若某波段训练标准差为 0，则将缩放因子置为 1。验证集、测试集和未标注区域只使用训练集得到的 (mu_d) 与 (sigma_d)，不重新估计参数。

### 3.4 PCA 光谱降维

在标准化后的训练中心像元上拟合 PCA。设训练协方差矩阵的前 $B$ 个特征向量构成投影矩阵 $W_B\in\mathbb{R}^{D\times B}$，则降维表示为

\[
Z=(\tilde{X}-m_{PCA})W_B,
\]

其中本文首个基线取 $B=15$，`whiten=false`。拟合结束后，将相同投影分块应用于完整影像，得到 610×340×15 的变换后数据立方体。`fair24_6_70` 训练子集上前 15 个主成分累计解释方差比为 0.999247。该数值仅描述训练数据方差保留程度，不代表分类准确率。

### 3.5 LDA 监督光谱降维

LDA 使用类别信息构造判别子空间。设第 $k$ 类训练样本均值为 $\mu_k$，全局训练均值为 $\mu$，则类内散度与类间散度可分别写为

\[
S_W=\sum_{k=1}^{C}\sum_{x_i\in k}(x_i-\mu_k)(x_i-\mu_k)^T,
\qquad
S_B=\sum_{k=1}^{C}n_k(\mu_k-\mu)(\mu_k-\mu)^T.
\]

LDA 寻找使类间差异相对于类内差异增大的投影方向。本文依照 scikit-learn 官方接口采用 `LinearDiscriminantAnalysis(solver="svd")` [9]；该求解器不显式计算协方差矩阵，适用于波段维数较高的输入。实现调用 `fit(\tilde{X}_{tr},y_{tr})`，其中光谱与标签均严格来自训练 split；随后仅调用 `transform` 处理验证、测试、背景和完整影像。由于 LDA 输出维数满足 $B\leq\min(D,C-1)$，Pavia University 的 $C=9$，故默认采用 $B=8$，输出 610×340×8 数据立方体。状态文件保存训练类别、训练均值、判别缩放矩阵与解释比，使推理阶段无需再次读取标签或重新拟合。

### 3.6 统一空间表示与模型输入

本文定义两类已实现输出：

1. `pixel`：在固定中心坐标处取得 $B$ 维光谱向量，构成 $N\times B$ 特征矩阵，供 SVM、XGBoost 或 MLP 使用；
2. `patch`：对完整变换后立方体进行边界填充，并以固定坐标为中心动态提取 $25\times25\times B$ 邻域。对 HybridSN，PyTorch 单样本张量整理为 $1\times B\times25\times25$，batch 形状为 $N_b\times1\times B\times25\times25$。

patch 不预先全部物化到磁盘，以避免重复存储。固定划分依据中心像元；邻域中不使用其他像元的类别标签。由于本文采用逐像元随机划分，相邻中心像元的 patch 可能重叠，此限制将在最终结果讨论中明确说明，并可在后续加入空间块划分作为严格泛化对照。

### 3.7 配置驱动的多路线框架

数据处理框架将“光谱处理—空间表示—模型”分离。当前实现的光谱处理为 `none`、`PCA` 和 `LDA`，空间表示为 `pixel` 和 `patch`；波段选择、LBP 与 Gabor 仍为后续扩展。YAML 同时保留 `pca:` 与 `lda:` 方法专属参数块，只需将 `reducer: pca` 改为 `reducer: lda`，即可由 PCA15 切换为 LDA8，而数据协议、随机种子、空间表示和 DataLoader 参数均保持不变。传统出口返回特征、零基模型标签、原标签、坐标和 sample ID；PyTorch 出口返回字典形式的输入张量及相同身份字段。不同方法通过独立配置指纹与冻结目录管理，由此可以共享固定测试样本并将预测映射回原图，同时避免统计预处理参数混用。

### 3.8 预处理状态与可追溯性

每个已拟合预处理状态记录数据集、split 协议、split seed、训练样本数、原始文件与 split 文件 SHA-256、标准化参数、PCA 分量及解释方差或 LDA 判别投影状态、patch 配置、软件版本和配置指纹。状态文件不包含第三方模型权重，也不复制完整 patch。不同 split 或不同配置产生不同目录，默认拒绝覆盖已有正式产物。

### 3.9 HybridSN 模型结构

以 PyTorch 的 `N×C×D×H×W` 维序表示输入，Pavia 基线张量为 `N×1×15×25×25`。网络连续使用三层无填充 3D 卷积：第一层有 8 个 `(7,3,3)` 卷积核，第二层有 16 个 `(5,3,3)` 卷积核，第三层有 32 个 `(3,3,3)` 卷积核。三层输出依次为 `N×8×9×23×23`、`N×16×5×21×21` 和 `N×32×3×19×19`。随后合并通道维与剩余光谱深度，将张量重排为 `N×96×19×19`，再以 64 个 `3×3` 2D 卷积核得到 `N×64×17×17`。展平后的 18,496 维特征依次通过 256、128 和 9 个神经元的全连接层；卷积层及两个隐藏全连接层后使用 ReLU，两个隐藏层后使用 Dropout 0.4。

网络不使用池化、BatchNorm 或数据增强。最终层直接返回九类 logits，并由 `CrossEntropyLoss` 联合完成 log-softmax 与负对数似然计算，因此模型内部不显式添加 Softmax。权重采用 Xavier/Glorot uniform 初始化，偏置初始化为零。该 Pavia 实例共有 4,844,793 个可训练参数；原论文参数表中的 5,122,176 对应 30 波段、16 类的 Indian Pines 实例，不能直接套用于 Pavia。核心模型只在 `src/models/HybridSN模型.py` 定义，数据处理与训练控制分别由数据模块和模型 Notebook 承担。

## 4 实验设计

### 4.1 论文兼容复现

首先在 `paper30` 上复现 HybridSN。预处理采用训练集专用标准化、PCA 15 维和 25×25 patch；模型训练轮数、batch size、优化器等尽量遵循原论文。原论文报告使用 Adam、batch size 256、100 epochs、无 batch normalization 与数据增强，并对 Pavia University 使用 25×25×15 输入 [1]。由于 `paper30` 没有独立验证集，本实验不得利用测试集选择最佳 epoch，而应采用预先固定的训练策略。

### 4.2 公平比较与调参

随后在 `fair24_6_70` 上进行模型选择。所有候选方法共享相同 train/validation/test；训练集用于拟合预处理和模型参数，验证集用于选择超参数与 checkpoint，测试集仅在配置冻结后评价。计划比较的路线包括：

- PCA15 + patch25 + HybridSN；
- PCA15 + pixel + SVM/XGBoost；
- 无降维 + pixel + SVM/XGBoost；
- LDA8 + pixel + SVM/XGBoost（数据路线已实现，分类实验待阶段 3）；
- 波段选择 + pixel/patch + 传统或深度模型（待实现）；
- PCA + LBP/Gabor + SVM/XGBoost（待实现）。

### 4.3 多随机种子与评价指标

当前数据划分与模型训练统一使用 seed=1442。后续稳健性实验在保留 seed1442 主复现的基础上，固定同一批额外种子并报告均值和标准差。评价指标包括 Overall Accuracy（OA）、Average Accuracy（AA）、Cohen's Kappa、每类准确率和混淆矩阵；横向比较同时报告参数量、训练时间和推理时间。不同 split 协议下的结果不进入同一公平比较表。

## 5 当前结果与待完成实验

数据、预处理与模型结构验收结果如下：

- Pavia University 有标签像元 42,776 个，两套固定划分均通过无交集、全覆盖、类别覆盖和同 seed 复现检查；
- `fair24_6_70` 上标准化、PCA 与 LDA 的拟合样本数为 10,265；LDA 只额外使用训练标签，验证/测试样本及标签均未参与拟合；
- PCA 输出数据立方体尺寸为 610×340×15，累计解释方差比为 99.9247%；
- LDA 输出数据立方体尺寸为 610×340×8；保存状态重载与重新拟合结果一致，且变换数值与 scikit-learn SVD-LDA 一致；
- patch DataLoader 的批输入形状已验证为 `batch_size×1×15×25×25`；
- 临时小网络已在 RTX 5070 上完成 `fair24_6_70` 全部 10,265 个训练样本与 2,567 个验证样本的 1 epoch 链路验收，测试集未用于该检查；
- HybridSN 逐层 shape、`N×9` logits、4,844,793 个参数与单 batch 交叉熵反向传播均已通过；
- 当前 77 项核心自动测试全部通过，覆盖 HybridSN 层配置、参数量、逐层 shape、前后向、输入契约，以及通用指标、空间回填、空间审计与训练/推理 traceability。

上述临时网络仍只用于验证批次、GPU、反向传播和验证只读过程，其诊断数值不属于论文分类结果。正式 HybridSN 采用训练 seed=1442、Adam 1e-3、batch 256、100 epochs 和固定最终 epoch；测试集仅在 checkpoint 固定后评测一次。结果为 OA 99.9532%、AA 99.8790%、Kappa 0.999380、测试损失 0.004360，29,944 个测试像元中误分 14 个。训练总计 494.36 s，端到端测试推理 2.359 s；checkpoint、逐轮日志、混淆矩阵、逐类精度、学习曲线和分类图均已保存。

需要强调，25×25 patch 的空间审计显示每个测试 patch 都包含训练中心像元且包含同类训练中心像元。因此该结果是可复现的随机像元 `paper30` baseline，但不能作为跨区域泛化证据。后续实验必须报告这一限制，并增加空间块划分对照。

### 5.1 课程作业要求与阶段完成度

本阶段已完成 PyTorch 环境、高光谱数据处理、PCA/LDA 工程实现、HybridSN 结构复现以及首个正式分类 baseline，已经形成基础训练—推理—结果闭环。教师提出的 HybridSN 改进、多随机种子和其他论文模型的多数据集比较仍属于后续实验。

| 课程任务 | 本阶段状态 |
|---|---|
| PyTorch 环境与 GPU 计算 | 已完成并验收 |
| 高光谱读取、划分与预处理 | 已完成并纳入全仓自动测试 |
| PCA/LDA 学习与使用 | 均已完成训练集专用实现、冻结状态与接口测试 |
| HybridSN 结构与流程 | 已完成原论文结构、两 Notebook 交接及 13 项模型测试 |
| HybridSN 分类 | 统一 seed=1442 的 100 epochs baseline 已完成，完整指标和图表齐全 |
| HybridSN 改进与消融 | 未开始 |
| 其他模型、多数据集比较 | 未开始；Houston 数据仍缺失 |

## 6 结论

本文完成了面向复现与多路线比较的高光谱数据处理基座、HybridSN 原论文结构及首个正式分类 baseline。通过冻结逐样本划分、限制统计变换拟合范围、统一样本身份并分离数据产物、模型定义与训练控制，后续可以在不改变测试集的前提下扩展传统方法、深度模型和消融路线。随机像元协议取得 99.99% OA，但空间重叠审计证明该结果具有明显乐观性；下一阶段将优先在 `fair24_6_70` 上完成多 seed 调参和空间块划分对照，再开展 HybridSN 改进与多模型公平比较。

## 参考文献

[1] S. K. Roy, G. Krishna, S. R. Dubey, and B. B. Chaudhuri, “HybridSN: Exploring 3-D–2-D CNN Feature Hierarchy for Hyperspectral Image Classification,” *IEEE Geoscience and Remote Sensing Letters*, vol. 17, no. 2, pp. 277–281, 2020, doi: 10.1109/LGRS.2019.2918719.

[2] I. T. Jolliffe and J. Cadima, “Principal component analysis: a review and recent developments,” *Philosophical Transactions of the Royal Society A*, vol. 374, no. 2065, 2016.

[3] R. A. Fisher, “The use of multiple measurements in taxonomic problems,” *Annals of Eugenics*, vol. 7, no. 2, pp. 179–188, 1936.

[4] T. Ojala, M. Pietikäinen, and T. Mäenpää, “Multiresolution gray-scale and rotation invariant texture classification with local binary patterns,” *IEEE Transactions on Pattern Analysis and Machine Intelligence*, vol. 24, no. 7, pp. 971–987, 2002.

[5] B. S. Manjunath and W. Y. Ma, “Texture features for browsing and retrieval of image data,” *IEEE Transactions on Pattern Analysis and Machine Intelligence*, vol. 18, no. 8, pp. 837–842, 1996.

[6] F. Melgani and L. Bruzzone, “Classification of hyperspectral remote sensing images with support vector machines,” *IEEE Transactions on Geoscience and Remote Sensing*, vol. 42, no. 8, pp. 1778–1790, 2004.

[7] T. Chen and C. Guestrin, “XGBoost: A scalable tree boosting system,” in *Proceedings of the 22nd ACM SIGKDD International Conference on Knowledge Discovery and Data Mining*, 2016, pp. 785–794.

[8] Scikit-learn developers, “LinearDiscriminantAnalysis,” *scikit-learn API Reference*, version 1.9, 2026. [Online]. Available: https://scikit-learn.org/stable/modules/generated/sklearn.discriminant_analysis.LinearDiscriminantAnalysis.html
