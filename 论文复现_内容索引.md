# 论文复现内容索引

> 本文档是「论文复现 + 多模型对比 + 多数据集验证」任务的**单文件索引**，汇总论文复现相关代码、配置、测试、脚本、结果与报告的位置（均使用绝对路径），并给出复现协议与一键复现命令。
>
> 复现对象：Zhang B., Zhao L., Zhang X. (2020) *"Three-dimensional convolutional neural network model for tree species classification using airborne hyperspectral images."* Remote Sensing of Environment, 247, 111938.

---

## 1. 复现的两个模型

| 模型 | 结构 | 论文参数量（B=125, C=12） | 输入 |
|---|---|---|---|
| **Paper3DCNN** | 五层有效 3D 卷积（kernel=(7,3,3)，无 padding/pooling）→ Dropout → Flatten(6080) → FC(128) → FC(C) | **951,652** | `[N,1,B,11,11]` |
| **Paper3D1DCNN** | 同一 3D 骨干 → 重排 `[N,64,B-30]` → 2×Conv1d(64→48→24, k=7) → Flatten(1992) → FC(C) | **225,292**（≈基础模型的 23.7%） | `[N,1,B,11,11]` |

- 关键约定：论文模型使用**原始光谱波段**（不做 PCA），空间 patch 固定 **11×11**；末端输出 **logits**（不加 Softmax），直接交给 `nn.CrossEntropyLoss`。
- 波段约束：Paper3DCNN 需 B > 30；Paper3D1DCNN 需 B > 42（五层有效光谱卷积把 B 减 30，两层 Conv1d 再减 12）。
- 逐层 shape 与参数量推导见模型文件 docstring。

---

## 2. 代码位置（绝对路径）

### 2.1 模型实现
- `G:\高光谱智能图像解译\实验交付\src\models\Paper3D1DCNN.py`
  包含 `Paper3DCNN`、`Paper3D1DCNN`、`_FiveLayer3DBackbone`（共用 3D 骨干）、`build_paper_model`（名称工厂）、`count_trainable_parameters`。
- `G:\高光谱智能图像解译\实验交付\src\models\改进Paper3D1DCNN.py`
  控制变量改进版 `ImprovedPaper3D1DCNN`：3D/1D 层加 **BatchNorm** + **全局平均池化（GAP）分类头**，无残差（曾因残差投影短路导致参数量反超，已移除）。

### 2.2 训练 / 评估 / 汇总脚本
- `G:\高光谱智能图像解译\实验交付\scripts\运行论文复现.py`
  统一训练+评估入口，按 `--model` 派发 `paper3dcnn / paper3d1dcnn / improvedpaper3d1dcnn / hybridsn / improved_hybridsn`；内置早停、checkpoint、`summary_row.json` 落盘。
- `G:\高光谱智能图像解译\实验交付\scripts\运行全部对比.py`
  15 项队列驱动（5 模型 × 3 数据集），顺序执行、断点续跑（按 `summary_row.json` 且 `max_epochs_configured > 1` 判完成）、单项失败不拖垮队列。
- `G:\高光谱智能图像解译\实验交付\scripts\汇总论文复现对比.py`
  扫描 `experiments/**/summary_row.json`（跳过 1-epoch 冒烟测试），按 (dataset, model) 去重，输出 `对比结果.csv/.md` 与三张图（对比指标、参数量-精度权衡、训练耗时）。

### 2.3 单元测试
- `G:\高光谱智能图像解译\实验交付\tests\test_paper_models.py`
  覆盖：参数量对齐（B=125/C=12 → 951,652 / 225,292）、输出 shape、无 Softmax/BN、三数据集波段类别数、反向传播梯度有限、工厂分派、波段下限校验、改进版更轻且含 BN。**16 项全部通过**。

### 2.4 配置文件
- 论文支路（原始波段 + patch11，`reducer: none`）：
  - `G:\高光谱智能图像解译\实验交付\configs\模型训练\Paper复现_Pavia.yaml`
  - `G:\高光谱智能图像解译\实验交付\configs\模型训练\Paper复现_IndianPines.yaml`
  - `G:\高光谱智能图像解译\实验交付\configs\模型训练\Paper复现_Salinas.yaml`
- HybridSN 支路（PCA15 + patch25，用于四模型对比）：
  - `G:\高光谱智能图像解译\实验交付\configs\模型训练\HybridSN对比_Pavia.yaml`
  - `G:\高光谱智能图像解译\实验交付\configs\模型训练\HybridSN对比_IndianPines.yaml`
  - `G:\高光谱智能图像解译\实验交付\configs\模型训练\HybridSN对比_Salinas.yaml`

---

## 3. 实验运行结果（本地 `experiments/`，默认不入库）

`.gitignore` 中 `experiments/*` 被忽略（训练产物体积大，仅保留本地）。已完成运行的 `summary_row.json` 位于：

| 数据集 | 模型 | OA | AA | Kappa | 参数 | 轮数 | 目录 |
|---|---|---|---:|---:|---:|---:|---:|---|
| Pavia University | Paper3DCNN | 0.9900 | 0.9837 | 0.9868 | 771,041 | 155/300 | `experiments\pavia_university__paper3dcnn__fair24_6_70__seed1442__20260818-2334` |
| Pavia University | Paper3D1DCNN | 0.9877 | 0.9795 | 0.9838 | 214,561 | 122/300 | `experiments\pavia_university__paper3d1dcnn__fair24_6_70__seed1442__20260818-2314` |
| Pavia University | 改进Paper3D1DCNN | 0.9802 | 0.9693 | 0.9738 | 201,993 | 167/300 | `experiments\pavia_university__improvedpaper3d1dcnn__fair24_6_70__seed1442__20260818-2359` |

（Indian Pines / Salinas 的 12 项仍在后台队列顺序运行中，完成后 `汇总论文复现对比.py` 会刷新下表与图。）

---

## 4. 报告与图

- 报告正文（含「一、(三)提高任务：复现论文」一节）：
  - `G:\高光谱智能图像解译\实验交付\report\实验报告-终稿.md`
  - `G:\高光谱智能图像解译\实验交付\report\实验报告-终稿.tex`（LaTeX 骨架，配合 pandoc 转 Word）
- 论文模型结果图（学习曲线 / 混淆矩阵 / 逐类精度 / 分类图）：
  - `G:\高光谱智能图像解译\实验交付\report\figures\13_paper3dcnn_*.png` … `16_paper3dcnn_*.png`
  - `G:\高光谱智能图像解译\实验交付\report\figures\17_paper3d1dcnn_*.png` … `20_paper3d1dcnn_*.png`
- 汇总对比产物：
  - `G:\高光谱智能图像解译\实验交付\report\对比结果\对比结果.csv`
  - `G:\高光谱智能图像解译\实验交付\report\对比结果\对比结果.md`
  - `G:\高光谱智能图像解译\实验交付\report\对比结果\对比指标.png`、`参数量精度权衡.png`、`训练耗时.png`

---

## 5. 复现协议（要点）

- **划分**：所有模型统一 `fair24_6_70`（24% 训练 / 6% 验证 / 70% 测试）+ `seed=1442`，中心像素身份一致。
- **超参只在 train/val 决定，test 仅在最后评测一次**（不碰测试集调参）。
- **论文支路**：SGD，lr=1e-4，momentum=0.9，batch=64，最多 300 轮，**loss 早停**（Δ < 0.003，patience 10）。
- **HybridSN 支路**：Adam，lr=1e-3，batch=256，最多 30 轮，accuracy 早停（patience 12）。
- **标准化**：仅训练集拟合的逐波段 z-score（`standard`）；论文支路 `reducer=none`，HybridSN 支路 `reducer=pca`（15 维）。
- 所有数字以实际运行为准；若与论文预期不符，如实分析原因，不编造。

---

## 6. 一键复现命令

```bash
# 进入项目根目录
cd "G:/高光谱智能图像解译/实验交付"

# 单模型单数据集（示例：Pavia 论文轻量模型）
python scripts/运行论文复现.py \
  --config "configs/模型训练/Paper复现_Pavia.yaml" \
  --model paper3d1dcnn

# 全部 15 项（5 模型 × 3 数据集，断点续跑）
python scripts/运行全部对比.py

# 汇总对比表与图
python scripts/汇总论文复现对比.py

# 单元测试（参数量对齐等 16 项）
python -m pytest tests/test_paper_models.py -q
```

---

## 7. 当前进度（截至 2026-08-19）

- 15 项队列 = 5 模型（Paper3DCNN / Paper3D1DCNN / 改进Paper3D1DCNN / HybridSN / ImprovedHybridSN）× 3 数据集（Pavia / Indian Pines / Salinas）。
- **已完成 3 项**：Pavia 三个论文模型（见 §3 表）。
- **排队 12 项**：Indian Pines、Salinas 的论文模型 + 全部 HybridSN 支路，后台队列运行中。
