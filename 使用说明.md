# 高光谱智能图像解译项目使用说明

## 1. 当前状态

阶段 2“数据划分与预处理”和阶段 3.2 首个 HybridSN baseline 已完成：

- Pavia University 两套 seed=1442 固定划分；
- 仅训练集拟合的标准化、PCA15 和 LDA8；
- pixel/patch、Dataset/DataLoader；
- 统计可视化和唯一 Notebook；
- GPU 数据链路冒烟验收；
- 当前核心自动测试覆盖数据划分、预处理、模型、训练和评价接口；
- 统一 seed=1442 的 `paper30` 正式 100 epochs 已完成，OA/AA/Kappa、性能、checkpoint、混淆矩阵、曲线和分类图齐全；
- 当前全仓 77 项自动测试通过。

随机像元 `paper30` 的 25×25 patch 空间重叠很强，结果只作为兼容口径 baseline；后续需要多 seed 和空间块划分对照。

## 2. 首次使用入口

### 2.1 激活项目环境

```powershell
Set-Location -LiteralPath '<你的仓库目录>'
.\scripts\激活项目环境.ps1
```

或者不激活，始终显式调用：

```powershell
.\.venv\Scripts\python.exe
```

当前开发机器直接复用已有 `.venv`，不要重复创建或覆盖数据。首次从 GitHub 克隆的协作者应按根目录 README 建立自己的 `.venv`，原始数据仍需自行放入 `data/raw/`。

### 2.2 检查环境

```powershell
.\.venv\Scripts\python.exe scripts\检查运行环境.py
```

应看到 Python 3.12.13、PyTorch 2.12.1+cu130、CUDA 可用和 RTX 5070。

### 2.3 打开两本分步 Notebook

```text
notebooks/高光谱数据预处理主流程.ipynb
notebooks/HybridSN模型结构学习.ipynb
```

在 VS Code/Jupyter 中选择：

```text
<仓库目录>\.venv\Scripts\python.exe
```

两本 Notebook 均已实际执行并保存输出。数据 Notebook 从上到下逐格执行，除冻结预处理主流程外，还包含参考仓库对应的伪彩色、Ground Truth、类别表、波段相关性和 PCA/LDA 对照；模型 Notebook 默认只读回放已完成的 100 轮正式实验，不会重新训练或再次评估测试集。

可直接用于 Word 报告的图表和 CSV 位于：

```text
results/notebook_outputs/reference_aligned/data_preprocessing/
results/notebook_outputs/reference_aligned/hybridsn_baseline/
```

如 Notebook 的参考对齐章节需要重新同步，可执行 `scripts/sync_reference_notebooks.py`；同步后再从头运行两本 Notebook。正式新实验仍推荐使用独立 CLI，以便完整保存 checkpoint、日志和运行清单。

### 2.4 查看光谱预处理对比实验

```text
notebooks/HybridSN预处理方法对比实验.ipynb
```

该 Notebook 已保存执行结果，直接打开即可查看五种预处理的汇总表、逐类精度、训练曲线和六联分类图。对应正式运行目录为：

```text
experiments/hybridsn_preprocessing_comparison__fair24_6_70__seed1442__<timestamp>/
```

重新运行同类对比使用：

```powershell
.\.venv\Scripts\python.exe scripts\运行HybridSN预处理对比.py
```

默认在带验证集的 `fair24_6_70` 协议上运行五条 30 epoch 路线；checkpoint 只由 validation OA 选择。报告级素材位于 `results/notebook_outputs/reference_aligned/preprocessing_comparison/`。

## 3. 一键检查数据处理

```powershell
.\.venv\Scripts\python.exe scripts\运行数据预处理.py
```

只运行到固定状态检查：

```powershell
.\.venv\Scripts\python.exe scripts\运行数据预处理.py --stop-after 3
```

主程序只读运行，不生成新 split、不重新拟合、不覆盖状态、不训练模型。

## 4. 配置与调参

配置统一位于：

```text
configs/数据预处理/
configs/模型训练/
```

详细参数表见 `configs/数据预处理/配置调参说明.md`。

修改或复制 YAML 后先运行：

```powershell
.\.venv\Scripts\python.exe scripts\检查配置.py `
  --config "configs\数据预处理\Pavia数据预处理.yaml" `
  --require-state
```

如果只改 batch size、loader seed 或 worker，不需要重新生成 PCA/LDA。若切换降维方法、修改分量数、标准化、patch 大小或表示方式，需要使用对应的新预处理状态。当前公开的 PCA15/LDA8 基准状态均已生成。

同一份新版 YAML 中同时包含 `pca:` 和 `lda:` 参数块。要从 PCA15 切到 LDA8，只改：

```yaml
reducer: pca
```

为：

```yaml
reducer: lda
```

Notebook 和默认脚本都读取同一个 `Pavia数据预处理.yaml`。切换路线时只编辑该文件中的 `split_protocol`、`reducer` 和 `representation`，不再更换文件名。

```powershell
.\.venv\Scripts\python.exe scripts\生成预处理状态.py `
  --config "configs\数据预处理\你的新配置.yaml"
```

默认拒绝覆盖已有产物。

## 5. 运行 HybridSN baseline

正式运行：

```powershell
.\.venv\Scripts\python.exe scripts\运行HybridSN基线.py
```

仅调试训练链路并保持测试集封存：

```powershell
.\.venv\Scripts\python.exe scripts\运行HybridSN基线.py `
  --epochs 1 --skip-test `
  --output-dir artifacts\hybridsn_smoke
```

正式输出目录自动包含配置、环境、日志、checkpoint、history、metrics、performance、预测、空间重叠审计、run manifest 和四类结果图。默认拒绝覆盖已存在的目录；从恢复点继续时同时传入 `--resume` 和原 `--output-dir`。

## 6. 核心目录

```text
实验交付/
├─ configs/       数据路线和未来模型训练 YAML
├─ data/          原始数据、固定划分、冻结预处理状态
├─ notebooks/     数据预处理与 HybridSN 结构学习入口
├─ scripts/       日常运行和复现验证入口
├─ src/           可复用核心代码
├─ tests/         当前核心自动测试
├─ docs/          论文、报告、决策和详细说明
├─ artifacts/     checkpoint 与验收日志
├─ results/       图表和数据审计结果
├─ experiments/   每次正式实验目录（默认不提交 Git）
└─ 归档/          已完成阶段的历史工具
```

## 7. `scripts/` 中应该运行什么

| 文件 | 作用 | 日常使用 |
|---|---|---|
| `检查运行环境.py` | 查看 Python/PyTorch/GPU | 按需 |
| `检查配置.py` | 只读校验 YAML | 每次改配置后 |
| `运行数据预处理.py` | 只读运行七步数据管线 | 推荐 |
| `运行HybridSN基线.py` | 正式训练、推理、评价、性能和结果图 | baseline/复现实验 |
| `生成预处理状态.py` | 为新数据路线拟合并保存状态 | 仅新路线 |
| `验证固定划分.py` | 独立验证两套固定 split | 复核时 |
| `验证预处理状态.py` | 独立重建并验证冻结状态 | 复核时 |

## 8. 测试

```powershell
.\.venv\Scripts\python.exe -m pytest
```

历史小网络、原始 split 生成器、一次性审计工具及其诊断日志仅在原开发机器本地归档，不属于公共仓库或日常入口。公共 `归档/` 只保留七份小型历史 YAML，用于验证统一配置的兼容性。

## 9. 下一阶段

下一阶段先在 `fair24_6_70` validation 上完成多 seed 调参与空间块划分对照，再实现 HybridSN 改进和消融。模型定义继续放在 `src/models/`，训练流程放在 `src/training/`，不得把正式训练循环写回数据 Notebook。
